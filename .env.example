# Sanity CMS — copy this file to .env and fill in your values
# Get these from sanity.io → your project → API settings

SANITY_PROJECT_ID=your_project_id_here
SANITY_DATASET=production

# Optional: read-only API token (needed for private datasets or server-side fetches)
# SANITY_API_TOKEN=your_read_only_token_here
