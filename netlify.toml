[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# ── 301 Redirects: old Webflow URLs → new Astro URLs ──────────────────────────
[[redirects]]
  from = "/post/*"
  to   = "/blog/:splat"
  status = 301
  force  = true
